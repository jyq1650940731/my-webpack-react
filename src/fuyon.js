

export function reduce1(x, y) {
    return x + y;
}

export function reduce(x, y) {
    console.log(1234);
    return x + y;
};

export function sdf(){
    console.log('ceshi')
}

// export function reduce2(x, y) {
//     return x + y;
// };

// export function reduce2(x, y) {
//     return x + y;
// };
// export function reduce3(x, y) {
//     return x + y;
// };